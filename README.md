# World Cup 2026 Predictor

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```

Fixtures are loaded from `fixtures.json`.
You can edit that file later to change the match list.

This app uses a local SQLite database (`worldcup_bets.db`) for predictions and leaderboard tracking.
